# Mise

Manages global tool versions with `mise`.

**YAML Key:** `mise`

**Properties:**
-   `id`: The tool to set globally (e.g., `go@latest`).

**Example:**
```yaml
mise:
  - id: go@latest
  - id: ruby@latest
```
