id: mise
name: Mise Config Provider
description: Manages mise dev tools version manager config.toml
version: 1.0.0
capabilities:
  - config_provider
execute:
  command: python
  args:
    - "${PLUGIN_DIR}/src/plugin.py"
